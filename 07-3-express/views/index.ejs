<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>index</title>
    <!-- title이라는 변수에 index(문자열)을 넣어 head.ejs를 불어온다 -->
    <%- include("./component/head.ejs",{title:'index'}) %>
</head>

<body>
    <!-- 다른 view 파일 include 불러오기 -->
    <%- include('./component/header') %>
    <h2>Hello EJS</h2>
    <% for(let i=0; i<5; i++){ %>
    <div>for문 <%=i%>번째</div>
    <% } %>

    <!-- btns 버튼 -->
    <% console.log(btns); %>
    <% for(let btn of btns) { %>
    <button type="button" class="btn btn-info"><%=btn%></button>
    <% } %>

    <% if(isLogin) { %>
    <h4>아직 로그인을 안했군요!</h4>
    <% } else { %>
    <h4>로그인 상태입니다.</h4>
    <div><%=me.name%></div>
    <div><%=me.msg%></div>
    <% } %>
    </body>

</html>